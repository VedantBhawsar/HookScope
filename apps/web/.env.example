# Generated by @hookscope/env — edit packages/env/src/docs.ts and re-run
# `bun run check:env generate` to update this file.

# ─── Server ───────────────────────────────────────────────
# Runtime environment
NODE_ENV=development

# ─── API ──────────────────────────────────────────────────
# Public URL of the API server (required)
NEXT_PUBLIC_API_URL=http://localhost:5000

# ─── Site ─────────────────────────────────────────────────
# Public URL of this dashboard
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Canonical site URL (metadata)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Public URL of the ingestion server (CSP connect-src)
NEXT_PUBLIC_INGESTION_URL=http://localhost:5001
